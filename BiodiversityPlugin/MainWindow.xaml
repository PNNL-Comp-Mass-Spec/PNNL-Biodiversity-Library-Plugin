<Window x:Class="BiodiversityPlugin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:BiodiversityPlugin.Models"
        xmlns:biodiversityPlugin="clr-namespace:BiodiversityPlugin"
        Title="Biodiversity Plugin" Height="900" Width="1100">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <SolidColorBrush x:Key="SolidBorderBrush" Color="#FFFFFF" />

        <SolidColorBrush x:Key="WhiteBrush" Color="White" />

        <SolidColorBrush x:Key="LightBrush" Color="CornflowerBlue" />

        <SolidColorBrush x:Key="DarkBrush" Color="Navy" />

        <SolidColorBrush x:Key="DisabledBackgroundBrush" Color="#EEE" />

        <SolidColorBrush x:Key="DisabledBorderBrush" Color="#AAA" />

        <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#888" />
        <biodiversityPlugin:OrgCodeToColorConverter x:Key="OrgCodeToColor" />
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Grid>
                            <Border 
            Name="Border"
            Margin="0,0,0,0" 
            Background="{StaticResource LightBrush}"
            BorderBrush="{StaticResource SolidBorderBrush}" 
            BorderThickness="1,1,1,1" >
                                <!--CornerRadius="2,12,0,0" -->
                                <ContentPresenter x:Name="ContentSite"
              VerticalAlignment="Center"
              HorizontalAlignment="Center"
              ContentSource="Header"
              Margin="12,2,12,2"
              RecognizesAccessKey="True"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Panel.ZIndex" Value="100" />
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource DarkBrush}" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0" />
                                <Setter Property="Foreground" Value="{StaticResource WhiteBrush}" />
                            </Trigger>
                            <!--<Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource DisabledBackgroundBrush}" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource DisabledBorderBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource DisabledForegroundBrush}" />
                            </Trigger> -->
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <TabControl TabStripPlacement="Left" Margin="0" Name="WizardTabs" SelectedIndex="{Binding SelectedTabIndex}">
        <TabItem Header="Organisms" Width="200" Height="150" FontSize="20" FontWeight="Bold" Foreground="White" BorderBrush="Black" BorderThickness="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="*" />
                <RowDefinition Height="15" />
                <RowDefinition Height="40" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" TextAlignment="Center" FontWeight="Bold" Foreground="Black" FontSize="20">Organisms</TextBlock>
                <biodiversityPlugin:MvvmTreeView Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" ItemsSource="{Binding Organisms}" SelectedItemTarget="{Binding SelectedOrganismTreeItem, Mode=TwoWay}">
                <biodiversityPlugin:MvvmTreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type models:OrgPhylum}" ItemsSource="{Binding OrgClasses}">
                        <TextBlock Text="{Binding PhylumName}" FontWeight="Bold" FontSize="16" />
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type models:OrgClass}" ItemsSource="{Binding Organisms}">
                        <TextBlock Text="{Binding ClassName}" FontWeight="Bold" FontSize="14"/>
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type models:Organism}">
                        <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="{Binding OrgCode, Converter={StaticResource OrgCodeToColor}}" />
                    </HierarchicalDataTemplate>
                </biodiversityPlugin:MvvmTreeView.Resources>
            </biodiversityPlugin:MvvmTreeView>
                <!--<Button Grid.Row="3" Grid.Column="0" FontSize="22"   Width="150" HorizontalAlignment="Center" Click="PreviousButton_OnClick">&lt; Previous</Button> -->
                <Button Grid.Row="3" Grid.Column="1" FontSize="20"   Width="150" HorizontalAlignment="Center" Command="{Binding NextTabCommand}" IsEnabled="{Binding IsOrganismSelected}">Next &gt;</Button>
            </Grid>
        </TabItem>


        <TabItem Header="Pathways"  Width="200" Height="150" FontSize="20" Foreground="White" FontWeight="Bold" BorderBrush="Black" BorderThickness="5" IsEnabled="{Binding IsOrganismSelected}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="*" />
                <RowDefinition Height="15" />
                <RowDefinition Height="40" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" TextAlignment="Center" FontWeight="Bold" Foreground="Black" FontSize="20">Pathways</TextBlock>
                <biodiversityPlugin:MvvmTreeView Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" ItemsSource="{Binding Pathways}" SelectedItemTarget="{Binding SelectedPathwayTreeItem, Mode=TwoWay}">
                <biodiversityPlugin:MvvmTreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type models:PathwayCatagory}" ItemsSource="{Binding PathwayGroups}">
                        <TextBlock Text="{Binding CatagoryName}" FontWeight="Bold" FontSize="16" />
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type models:PathwayGroup}" ItemsSource="{Binding Pathways}">
                        <TextBlock Text="{Binding GroupName}" FontWeight="Bold" FontSize="14"/>
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type models:Pathway}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20*" />
                                    <ColumnDefinition Width="80*" />
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" IsChecked="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" /> <!-- Checked="{Binding IncChecked}" Unchecked="{Binding DecChecked}"/> -->
                                <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="12" FontWeight="Regular" />
                            </Grid>  
                        </HierarchicalDataTemplate>
                    </biodiversityPlugin:MvvmTreeView.Resources>
                    <biodiversityPlugin:MvvmTreeView.InputBindings>
                        <MouseBinding Command="{Binding NextTabCommand}" MouseAction="LeftDoubleClick" />
                    </biodiversityPlugin:MvvmTreeView.InputBindings>
                </biodiversityPlugin:MvvmTreeView>
                <Button Grid.Row="3" Grid.Column="0" FontSize="20" Width="150" HorizontalAlignment="Center" Command="{Binding PreviousTabCommand}">&lt; Previous</Button>
                <Button Grid.Row="3" Grid.Column="1" FontSize="20" Width="150" HorizontalAlignment="Center" Command="{Binding ExportToSkylineCommand}" IsEnabled="{Binding IsPathwaySelected}">Next &gt;</Button>

                
            </Grid>
        </TabItem>

        <TabItem Header="Export to Skyline"  Width="200" Height="150" FontSize="20" 
                 FontWeight="Bold" BorderBrush="Black" BorderThickness="5" Foreground="White"
                 IsEnabled="False">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition Height="15" />
                    <RowDefinition Height="30" />
                    <RowDefinition Height="30" />
                    <RowDefinition Height="30" />
                    <RowDefinition Height="15" />
                    <RowDefinition Height="18*" />
                    <RowDefinition Height="15" />
                    <RowDefinition Height="40" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Grid.ColumnSpan="2" TextAlignment="Center" FontWeight="Bold" Foreground="Black" FontSize="20">Data for Export</TextBlock>
                <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Foreground="Black" Grid.Row="2" FontSize="14" Text="{Binding SelectedOrganismText}" />
                <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Foreground="Black" Grid.Row="3" FontSize="14" Text="{Binding SelectedPathwayText}" />
                <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Foreground="Black" Grid.Row="6" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="30">
                    No proteins found for this<LineBreak/>
                    Organism/Pathway combination
                </TextBlock>
                <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="6" Background="White" VerticalAlignment="Stretch" Visibility="{Binding IsQuerying, Converter={StaticResource BoolToVis}}" />
                <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Foreground="Black" Grid.Row="4" FontSize="14" Text="{Binding NumProteinsText}" Visibility="{Binding VisibleProteins}" />
                <DataGrid Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="6" FontSize="14" AutoGenerateColumns="False" IsReadOnly="True" RowHeaderWidth="0" ItemsSource="{Binding FilteredProteins}" Visibility="{Binding VisibleProteins}">
                    <DataGrid.Columns>
                        <DataGridTextColumn FontSize="14" Foreground="Black" Header="Accession" Width="90" Binding="{Binding Accession}" FontWeight="Regular" IsReadOnly="True"></DataGridTextColumn>
                        <DataGridTextColumn FontSize="14" Foreground="Black" Header="Protein name" Width="30*" Binding="{Binding Name}" FontWeight="Regular" IsReadOnly="True"></DataGridTextColumn>
                        <DataGridTextColumn FontSize="14" Foreground="Black" Header="Protein Description" Width="70*" Binding="{Binding Description}" FontWeight="Regular" IsReadOnly="True"></DataGridTextColumn>
                        <!-- <DataGridTextColumn FontSize="14" Foreground="Black" Header="Protein Description" Width="10*" Binding="{Binding Description}" FontWeight="Regular" IsReadOnly="True"></DataGridTextColumn> -->
                    </DataGrid.Columns>
                </DataGrid>

                <Button Grid.Column="0" Grid.Row="8" Width="150" FontSize="20" Command="{Binding PreviousTabCommand}">&lt; Previous</Button>
                <Button Grid.Column="1" Grid.Row="8" Width="150" FontSize="20" Command="{Binding ExportToSkylineCommand}" >
                    <StackPanel Orientation="Horizontal">
                        <Image Source="DataFiles/skyline_logo.jpg" Width="60" Height="30"></Image>
                        <TextBlock FontSize="20" VerticalAlignment="Center">Export</TextBlock>
                    </StackPanel>
                </Button>
                <ScrollViewer Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="5" Grid.RowSpan="3" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <Canvas x:Name ="PathwayImage" Background="{Binding PathwayImage}"  Visibility="{Binding PathwayVisibility}"></Canvas>
                </ScrollViewer>
                <Border Opacity=".8" Grid.Row="0" Grid.RowSpan="9" Grid.ColumnSpan="2" BorderBrush="Black" BorderThickness="1" Background="DimGray" VerticalAlignment="Stretch" Visibility="{Binding IsQuerying, Converter={StaticResource BoolToVis}}">
                    <TextBlock VerticalAlignment="Center" Margin="200" Foreground="AntiqueWhite" FontSize="36" FontWeight="Bold"
                       Text="{Binding Path=QueryString, UpdateSourceTrigger=PropertyChanged}">
                    </TextBlock>
                </Border>
                <!-- <Frame Grid.Row="4" Grid.Column="0" Grid.RowSpan="3" Grid.ColumnSpan="2" Source="{Binding Path=MapUrl}" Visibility="{Binding Path=UrlVisible}"></Frame> -->
                <!-- <WebBrowser Grid.Row="4" Grid.Column="0" Grid.RowSpan="3" Grid.ColumnSpan="2" Tag="{Binding Path=MapUrl}" Visibility="{Binding Path=UrlVisible}"></> -->
            </Grid>
 
            <!-- <Button Grid.Row="3" Width="130" HorizontalAlignment="Right" Margin="0,10,0,0" Command="{Binding ExportToSkylineCommand}">Export to Skyline</Button>
        -->
        </TabItem>
 
    </TabControl>
</Window>
