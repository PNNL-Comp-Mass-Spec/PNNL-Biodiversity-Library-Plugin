<Window x:Class="BiodiversityPlugin.UpdateExistingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BiodiversityPlugin"
        xmlns:utilities="clr-namespace:KeggDataLibrary.Utilities;assembly=KeggDataLibrary"
        xmlns:models="clr-namespace:KeggDataLibrary.Models;assembly=KeggDataLibrary"
        mc:Ignorable="d"
        Title="Update Existing Window" Height="240" Width="545">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="75"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" HorizontalAlignment="Center" TextWrapping="Wrap" Margin="5,5,5,0">
            If the data provided by the Biodiversity Plugin was not sufficient, users can customize their data by 1.) Replacing the 
            existing organism with entirely 2.) Supplementing an existing organism with additional data or 3.) Add a new organism
            that does not yet exist in the current database. To perform any of these tasks, user must input two files. The first file 
            is the .blib file. The second is the mzIdentML PSM results file.   </TextBlock>

        <TextBlock Grid.Column="0" Grid.Row="1" Text=".blib File Location" FontWeight="Bold" HorizontalAlignment="Center"/>
        <TextBlock Grid.Column="0" Grid.Row="2" Text="mzIdentML Folder Location" FontWeight="Bold" HorizontalAlignment="Center"/>
        
        
        <TextBox Grid.Column="1" Grid.Row="1" Margin ="5,5" BorderThickness="1" BorderBrush="Black" Text="{Binding BlibPath}" IsReadOnly="True"/>
        <Button Grid.Column="2" Grid.Row="1" Width="60" Height="30" BorderThickness="1" BorderBrush="Black" Command="{Binding SelectBlibCommand}">
            <Image Source="DataFiles/search-icon.png"/>
        </Button>
        
        <TextBox Grid.Column="1" Grid.Row="2" Margin ="5,5" BorderThickness="1" BorderBrush="Black" Text="{Binding MsgfPath}" IsReadOnly="True"/>
        <Button Grid.Column="2" Grid.Row="2" Width="60" Height="30" BorderThickness="1" BorderBrush="Black" Command="{Binding SelectMsgfCommand}">
            <Image Source="DataFiles/search-icon.png" />
        </Button>

        <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" HorizontalAlignment ="Left" Width="130" Height="30" Margin="40,0,0,0" Content="Replace Organism" IsEnabled="{Binding StartButtonEnabled}" Command="{Binding SelectButtonCommand}"/>
        <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" HorizontalAlignment="Center" Width="130" Height="30" Content="Supplement Organism" IsEnabled="{Binding StartButtonEnabled}" Command="{Binding SelectButton2Command}"/>
        <Button Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Right" Width="130" Height="30" Margin="0,0,40,0" Content="Add New Organism" IsEnabled="False" Command="{Binding }"/>

    </Grid>
</Window>
